<div class="article_comment">
  <%= "#{comment.commenter} · ##{comment.id} · #{comment.created_at.strftime("%Y年%m月%d日")}" %>
<%= link_to 'Destroy comment' , ['blog',comment.article.user,comment.article, comment] , method: :delete ,data: { confirm: 'Are you sure?' } %>
<p>
  <%= comment.body %>
</p>

</div>